---------- FIRST PRIORITY ----------
-find where the bot could life (given that it might need to be updated/restarted)
- Test on someone if everything is intuitive
---------- SECOND PRIORITY ----------
- adjust /clear command. It needs to also show start message (basically /clear + /start)
- look at admin commands that need an update to make them more convenient. Do not forget to update documentation afterwards.
        * /users
- see if adding /clear command to tutorials will make user experience better.
- remove redundant functions from database.py. Leave only the most general ones.
- think of more convenient way to access people, now it's by telegram_id which is inconvenient (use ID)
- refactor commands code. Now it's redundant.
- write technical documentation
- get holders for papers for keychains (to write what key has which id)
---------- THIRD PRIORITY ----------
- make key IDs representing  more reliable in build_key_obtained_mailbox_key_choice_keyboard (take them from database)
- Add another Bot which will serve as a testing/learning ground for admins.
- Reengineer the way handlers are stored

---------- Temporary information ----------
- Add admin convenient usage of admin features
        * /givekey key_id telegram_id (X)
        * /mytelegramid # shows user telegram id (X)
        * /clear (X)
        * /users [name] [surname] [room] # information about all users for given query (X)
        * /adduser telegram_id name surname room [telegram_name] [phone_number] (X)
        * /keyhistory key_id [last n records] (X)
        * /updateuser telegram_id option value [option value ...] (X)
        * /keystatus key_id # full information about key status (current holder, owner, isActivated) (X)
        * /activatekey key_id (X)
        * /deactivatekey key_id (X)

        * /keys # general information about keys
        * /deleteuser telegram_id
        * /help